import 'package:flutter/material.dart';

class ForgotPasswordScreen extends StatefulWidget {
  const ForgotPasswordScreen({super.key});
  @override
  State<ForgotPasswordScreen> createState() => _ScreenNameState();
}
class _ScreenNameState extends State<ForgotPasswordScreen> {
  // 1️⃣ VARIABLES (state)
  // 2️⃣ INIT (runs once)
  // 3️⃣ FUNCTIONS (logic)

  @override
  Widget build(BuildContext context) {
    // 4️⃣ UI
    return Scaffold();
  }
}
