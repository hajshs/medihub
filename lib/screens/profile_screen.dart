import 'package:flutter/material.dart';

class ProfileScreen extends StatefulWidget {
  const ProfileScreen({super.key});
  @override
  State<ProfileScreen> createState() => _ScreenNameState();
}
class _ScreenNameState extends State<ProfileScreen> {
  // 1️⃣ VARIABLES (state)
  // 2️⃣ INIT (runs once)
  // 3️⃣ FUNCTIONS (logic)

  @override
  Widget build(BuildContext context) {
    // 4️⃣ UI
    return Scaffold();
  }
}