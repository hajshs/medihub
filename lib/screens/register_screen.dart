import 'package:flutter/material.dart';

class RegisterScreen extends StatefulWidget {
  const RegisterScreen({super.key});
  @override
  State<RegisterScreen> createState() => _ScreenNameState();
}
class _ScreenNameState extends State<RegisterScreen> {
  // 1️⃣ VARIABLES (state)
  // 2️⃣ INIT (runs once)
  // 3️⃣ FUNCTIONS (logic)

  @override
  Widget build(BuildContext context) {
    // 4️⃣ UI
    return Scaffold();
  }
}